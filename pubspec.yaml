name: glassvault
description: Interact with glassvault
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1
environment:
  sdk: ">=2.12.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  get: ^4.3.8
  filesize: ^2.0.1
  web3dart: ^2.3.2
  solana: ^0.13.3+1
  shared_preferences: ^2.0.8
  hive_flutter: ^1.1.0
  hive: ^2.0.4
  crypto: ^3.0.1
  encrypt: ^5.0.1
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4
flutter:
  uses-material-design: true